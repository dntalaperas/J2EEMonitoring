$(document).ready(function(){
    setTimeout(function() {
      // Lazy init ....
      /* ---------- DateRangepicker for Bootstrap ---------- */
 $('#daterange').daterangepicker();
}, 3000);
});